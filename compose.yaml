services:
  app:
    container_name: nest-app
    image: my-nest-prod
    build:
      context: .
      dockerfile: Dockerfile
      target: prod
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=production
    restart: unless-stopped
